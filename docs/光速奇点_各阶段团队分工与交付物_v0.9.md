# 《光速奇点》各阶段团队分工与交付物

> 版本：v0.9
> 更新日期：2026 年 7 月 27 日
> 项目周期：2026 年 7 月 13 日—2026 年 9 月 5 日
> 继续开发：2026 年 9 月 5 日后补齐完整设计
> 成员：陈俊贤、潘陈俣、张梓涵

---

# 1. 长期职责

## 陈俊贤：技术与核心架构

负责：

- 模块解耦；
- 公共协议；
- 核心运行协调；
- 网格；
- 世界查询；
- 光线和光粒执行基础；
- 占用和放置事务；
- 运行状态；
- 关卡对象注册和校验；
- 日志、自检和快照；
- 存档、成就技术基础；
- Git、AI任务和集成。

不负责单独决定所有机关规则，也不包办全部机关实现和关卡内容。

## 潘陈俣：玩法系统与机关

负责：

- 机关规则；
- 光线和光粒交互；
- 方向和非法入射；
- 速度；
- 颜色；
- 水晶条件；
- 机关脚本和场景；
- 玩法测试；
- 组合规则验收。

不只提供想法，必须提交可运行、可配置和可测试的模块。

禁止越界：

- 不自行修改核心坐标换算；
- 不修改运行状态协议；
- 不修改 Placement 事务语义；
- 不绕过 Occupancy/WorldQuery；
- 不为单个机关复制一套核心系统。

需要新增核心接口时，向陈俊贤报告，由陈俊贤统一并入公共契约。

## 张梓涵：关卡、美术与内容

负责：

- TileMapLayer地图；
- 可放置区域；
- 固定对象布局；
- 关卡原型和正式关卡；
- 美术资源和Profile；
- UI文字；
- 科学家与成就内容；
- 资源来源；
- 逐关测试。

不修改核心传播、占用、状态和重置代码。

不要求张梓涵承担以下工作：

- 编写 GDScript；
- 手写 UID；
- 维护复杂 Dictionary；
- 修改 Registry；
- 直接编辑资源文本；
- 用 `Node.name` 管理对象身份。

张梓涵使用唯一正式美术 Profile 插件（`addons/light_speed_art_profile/`，当前唯一的正式 EditorPlugin）、Godot 内置 Inspector 配置入口，以及已完成的方法 B 关卡编辑能力（Godot 原生 GUI 关卡编辑基础）完成配置，重点验证机关视觉、锚点和格子布局。其中 Inspector 是 Godot 内置配置入口而非第二个插件；方法 B 与 Godot 原生 GUI 关卡编辑基础已完成；第二正式 EditorPlugin（D7B / 关卡工具插件）已永久取消，不延期，当前不存在也不再有第二个正式插件。

---

# 2. 已完成基线

截至 2026 年 7 月 27 日：

- Diagnostics foundation 已合并 main；
- `RuntimeLogger`、`DiagnosticsController`、自检基础和 `RuntimeSnapshotData` 工具链已实现；
- 七项启动自检已真实运行，编排由 `StartupSelfCheckCoordinator` 整块承担；
- 当前真实运行状态为五状态（SETUP、READY_TO_FIRE、PULSE_ACTIVE、MOVE_WINDOW、COMPLETED；M4-F1 同步）；
- 运行状态所有权已迁出核心：`RunStateController`（`gameplay/interaction/run_state_controller.gd`，`RefCounted`）；
- D3 运行编排解耦冲刺已在 `refactor/kud-runtime-orchestration` 基本完成：`LevelRuntimeController`（`gameplay/runtime/`）唯一持有 `pulse_generation`、`runtime_moves_used`、发射编排、异步过期与 R 顺序；`core_loop_prototype.gd` 已降至约 480 物理行，只负责场景引用、依赖组装、输入转发、右键入口、节点工厂、UI 适配与启动接线；
- 已迁出模块：`LevelWorldQuery`、`LightWorldQuery`、`RayMechanismAdapter`、`RayExecutionModule`、`FireRequest`、`FixedEmitter`、`LightVisualController`、`LevelObjectRegistry`、`ObjectiveController`、`InventoryController`、`PlacementController`、`DragFlowController`、`DragContext`、`PlayerInteractionController`、`StartupSelfCheckCoordinator`；
- `BasicCrystal` 拥有显式 `crystal_id`（当前原型场景为 `crystal_001`），禁止用 `Node.name` 作正式 ID；
- 移动使用原子占用迁移，回收使用库存归还预留；
- ~~`READY_TO_FIRE`、开始运行入口、光粒（Particle Runtime）、Runtime 自动 Validation Gate 仍未实现~~（均已实装：D7-1/D7-2/D7-3 五态与正式入口、D7-4 光粒运行时、M4-E4 `Q` 切换）；统一 `EmissionRequest`/`LightRuntimeCoordinator` 仍为目标；`LevelValidator` v0（D6）已完成；`GridPlacedObject` 基础与位置契约（D3A）已实现（`position` 为场景编辑事实，`cell` 由 `position` 派生，`cell↔position` 换算边界已建立），不再标记为待重构；
- 已实现：`EmitterConfigNode`（D3B）负责发射器编辑器配置，`EmissionPreview`（D3C）提供发射预览，真实光线运行接线已有基础；
- Godot 原生负责节点拖动、复制、64×64 网格吸附和 position Undo/Redo，不另建吸附插件。

当前阶段状态（D7-0 收口）：

- D5（正式关卡模板与四层 `TileMapLayer`）、D6（`LevelValidator` v0）、编辑器方法 B、Godot 原生 GUI 关卡编辑基础、墙体方向编辑（Wall Directional Authoring，四视觉轴 ─ │ / \ 在 gameplay 上完全等价）均已完成；
- 仍未实现（M4-F1 同步）：统一 `EmissionRequest` / `LightForm` / 光运行公共层、玩家实际八方向运行控制（`W` 已冻结但未接线）、多 Crystal 正式关卡合同、真实 `RuntimeSnapshot` 采样、新机关批次、完整提示 UI；~~`READY_TO_FIRE` 与正式“开始运行”入口~~（已实装 D7-1/2/3）、~~Runtime 自动 Validation Gate~~（已实装 D7-1/3）、~~Particle Runtime / 玩家 RAY/PARTICLE 切换 / Particle 八方向~~（已实装 D7-4 / M4-E1~E4）；
- 第二正式 EditorPlugin（D7B / 关卡工具插件）已永久取消，不延期。

此前已完成并保留：

- 64×64世界格；
- `Vector2i`；
- 普通脉冲原型；
- 墙体；
- 基础水晶；
- 单格镜面；
- 轻量占用表；
- 单槽库存；
- 拖拽、放置、移动、回收；
- 运行状态；
- 运行期直接移动次数；
- R完整重置；
- 水晶、镜面、机关栏和光路的部分视觉Profile；
- Git、MCP、AI审查流程。

后续采用行为保持式抽离，不从零重写。

---

# 3. 阶段0：决策冻结与分支收尾

**时间：7月22日晚—7月24日**

## 陈俊贤必做

- 完成当前视觉分支收尾；
- 同步并验证 `main`；
- 将新玩法权限写入正式文档；
- 冻结目录和公共接口；
- 建立重构前回归清单；
- 记录现有核心脚本行为。

### 附加

- 补齐机关栏正式图标；
- 补齐光线路段四类方向纹理（水平、垂直、/、\，覆盖八方向传播）；
- 预先建立Issue模板。

## 潘陈俣必做

- 输出必做机关规则表；
- 标明光线、光粒适用范围；
- 输出配置锁定和重置规则；
- 给每个必做机关准备测试用例。

### 附加

- 输出长镜、分光器、滤光片等完整规则草案。

## 张梓涵必做

- 整理64×64素材；
- 验证现有Profile；
- 列出GUI关卡编辑需求；
- 记录当前拖动、复制、配图问题。

### 附加

- 预制发射器光线/光粒两套外观；
- 预制墙体TileSet。

## 阶段交付

- 可运行基线；
- 冻结决定；
- 文档集；
- 重构前测试清单；
- 任务列表。

---

# 4. 阶段1：核心解耦与编辑器基础

**时间：7月25日—8月4日**

## 当前进度切片

已完成：

- Diagnostics foundation；
- 日志与七项启动自检基础（编排由 `StartupSelfCheckCoordinator` 承担）；
- 运行状态所有权迁出核心：`RunStateController` 已实现并接入 `core_loop_prototype`，四态不变，不含 `READY_TO_FIRE`；
- 世界只读查询迁出：`LevelWorldQuery` / `LightWorldQuery` / `RayMechanismAdapter` / `RayExecutionModule` / `FireRequest` / `FixedEmitter`；
- 运行期编排迁出：`LevelRuntimeController` 持有 `pulse_generation` / `runtime_moves_used` / 发射 / 异步过期 / R 顺序；
- 交互与放置迁出：`PlayerInteractionController` / `DragContext` / `DragFlowController` / `PlacementController` / `InventoryController` / `OccupancyRegistry`（原子单格移动与归还预留）；
- 视觉迁出：`LightVisualController`；
- 水晶身份与目标迁出：`BasicCrystal` 显式 `crystal_id` / `LevelObjectRegistry` / `ObjectiveController`；
- `core_loop_prototype.gd` 已降至约 480 物理行。

后续待推进（不在 D3-F2 范围）：

- `GridPlacedObject` 基础与位置契约（D3A）已实现（position 是场景编辑事实，cell 由 position 派生，`get_cell()`=`world_to_cell(position)`，`set_cell()`=`cell_to_world(cell)`），不再列入待推进；编辑器方法 B 与完整 PackedScene 模板工作流已由 D5 完成；仍尚未完成的是多格对象完整编辑流程；
- Godot 原生负责节点拖动、复制、64×64 网格吸附和 position Undo/Redo，不另建吸附插件；
- Runtime 自动 Validation Gate（运行期自动调用 `LevelValidator`；`LevelValidator` v0 本身已完成）；
- `READY_TO_FIRE` 与开始运行入口；
- 统一 `EmissionRequest` / `LightRuntimeCoordinator` / 光粒（Particle Runtime）；
- 真实 `RuntimeSnapshot` 采样时点与 schema；
- 顺序组、同时组与完整多水晶正式关卡。

D3B `EmitterConfigNode` 与 D3C `EmissionPreview`/真实光线接线已实现，真实光线运行接线已有基础，不再列入待推进。

本阶段不同时实现全部统一光运行架构。

以下为阶段1计划目标，保留作为长期顺序参考：

## 陈俊贤必做

按小步迁移：

1. 公共状态和协议；
2. 通用发射请求；
3. 光运行协调器；
4. 世界只读查询；
5. 现有普通光线抽离；
6. 固定发射器；
7. `GridPlacedObject` 基础与位置契约（D3A，已实现）；
8. 关卡对象注册和校验；
9. 开始运行按钮；
10. 日志与自检基础；
11. 轻量库存抽离；
12. 玩家操作最后抽离。

每一步完成后回归当前行为。

后续阶段顺序与分工：D2.6 文档重扫 → D3A `GridPlacedObject` 基础与位置契约（陈俊贤，已实现）→ D3B `EmitterConfigNode`（陈俊贤）→ D3C `EmissionPreview` 与真实光线接线（陈俊贤）→ D4 `BasicCrystal` 接入 → D5 模板和四层 TileMapLayer（陈俊贤架构、张梓涵绘制）→ D6 `LevelValidator`（陈俊贤）→ D7A 美术配置插件（陈俊贤）→ D7C 张梓涵 GUI 验收 → D8 解耦、回归与文档收口（陈俊贤）。其中 D3A/D3B/D3C/D4/D5/D6/D7A 均已完成，D7C（Godot 原生 GUI 关卡编辑基础）已完成；D7B 关卡工具能力 / 第二正式 EditorPlugin 已永久取消，不延期。Godot 原生负责节点拖动、复制、64×64 网格吸附和 position Undo/Redo，不另建吸附插件；运行时代码不得依赖 `addons/`；当前只有一个正式 EditorPlugin（`addons/light_speed_art_profile/`），其 Dock、Panel、Resolver、Browser、Service 等均为插件内部模块，方法 B 与 Godot 原生 GUI 关卡编辑基础已完成，不再新增第二个正式插件；插件全部关闭后游戏正常运行。

### 附加

- JSON状态快照；
- Debug游戏内控制台；
- 多格对象基础接口；
- `LevelData` Resource。

## 潘陈俣必做

- 将基础镜面适配公共接口；
- 参与世界交互事件定义；
- 完成最简单的新机关的脚本/场景或规则验收；
- 不直接修改核心循环。

### 附加

- 提前实现加速器和减速器原型。

## 张梓涵必做

- 验证拖动、复制、吸附；
- 验证发射器、墙体、水晶的GUI配置；
- 建立关卡模板；
- 制作至少一个灰盒测试场景；
- 验证不改脚本即可替换主要美术。

### 附加

- 制作单机关原型库；
- 初步制作三章节的灰盒布局。

## 阶段交付

- 精简核心循环；
- 普通光线通过独立模块运行；
- 固定发射器；
- GUI 64 格吸附：Godot 原生 64×64 网格吸附 + D3A `GridPlacedObject` 位置契约（position 为场景编辑事实，cell 由 position 派生，已实现）；编辑器方法 B（2D 视图拖动、64×64 吸附、写回逻辑 cell）与完整模板工作流已由 D5 完成；
- 关卡模板；
- 至少一个新机关接入不修改核心循环；
- 灰盒关卡可运行。

---

# 5. 阶段2：三条线并行第一批

**时间：8月5日—8月16日**

## 陈俊贤必做

- 光粒状态；
- 整数Tick；
- 光粒执行模块；
- 接入统一协调器；
- 安全批次移动基础；
- 完成库存和玩家操作解耦；
- 完善关卡校验；
- 每2—3天集成。

### 附加

- 通用MoveRequest；
- 多光粒同Tick排序；
- 性能统计；
- 详细状态快照。

## 潘陈俣必做

- 基础镜面稳定版；
- 光粒加速器；
- 光粒减速器；
- 一种光粒专属墙体/交互墙；
- 光形态目标规则；
- 每个机关独立测试场景。

### 附加

- 长镜面；
- 墙体消除器；
- 速度检测门。

## 张梓涵必做

- 第一批机关素材；
- 发射器两套外观；
- 墙体与TileSet；
- 单机关原型；
- 机关组合原型；
- 灰盒章节关卡；
- 编辑器问题反馈。

### 附加

- 每章约两个灰盒关卡；
- 更完整运行反馈和教学标识。

## 阶段交付

- 光线和光粒统一入口；
- 第一批机关；
- 至少两个组合原型；
- 三章节均有灰盒内容；
- 模块接口无明显反向依赖。

---

# 6. 阶段3：第二批模块、组合验证与功能冻结

**时间：8月17日—8月24日**

## 陈俊贤必做

- 统一交互事件；
- 目标系统最小接口；
- 循环和安全上限；
- 运行期移动稳定；
- 在现有 Diagnostics foundation 上补强：真实 `RuntimeSnapshot` 玩法采样、更完整轮转或调试能力、与未来模块接线（日志轮转、自检和状态快照基础已实现，不再从零开始）；
- 关卡加载；
- 集成Bug修复。

Debug 游戏内控制台与完整真实采样仍是未实现附加项。

### 附加

- 多格机关完整占用；
- 同时/顺序窗口基础；
- 特殊持续光线全量重算。

## 潘陈俣必做

从已稳定模块中完成一批：

- 分光器；
- 滤光片；
- 颜色水晶；
- 光形式转换器。

进度不足时优先保证必做内容稳定。

### 附加

- 同时水晶；
- 顺序水晶；
- 光电二极管；
- 受控机关。

## 张梓涵必做

- 三章节灰盒关卡；
- 将稳定组合转为候选正式关卡；
- 主要机制视觉；
- 教学顺序；
- 科学家资料和3项成就内容初稿；
- 逐关测试记录。

### 附加

- 更多组合原型；
- 更多章节装饰；
- 9月5日后关卡储备。

## 8月24日出口

- 功能冻结；
- 不新增高风险底层系统；
- 未通过测试的附加模块不进入正式关卡；
- 三章节结构保持完整。

---

# 7. 阶段4：正式内容与首个候选版本

**时间：8月25日—8月29日**

## 陈俊贤必做

- 主界面；
- 选关；
- 关卡切换；
- 通关；
- 最小存档；
- 成就弹窗；
- 成就展示页；
- 第一份Windows候选包。

### 附加

- 暂停与设置完善；
- 详细科学家档案分类；
- 构建信息页面。

## 潘陈俣必做

- 审查正式关卡规则；
- 验证预期解法；
- 修复机关Bug；
- 不新增高风险机关。

### 附加

- 对附加机关制作独立可选关卡，不阻塞主线。

## 张梓涵必做

- 将灰盒转为正式关卡；
- 三章节各保留代表关卡；
- 正式视觉；
- 教学提示；
- 1位重点科学家；
- 3项正式成就；
- 来源和授权。

### 附加

- 每章扩展至约2关；
- 增加更多成就和科学家内容。

## 阶段交付

- 可完整启动、选关、通关、解锁成就和保存进度的Windows候选版本。

---

# 8. 阶段5：冻结、回归与交付

**时间：8月30日—9月4日**

## 三人必做

- 逐关回归；
- 连续R；
- 连续发射；
- 光线/光粒；
- 机关组合；
- 成就；
- 存档；
- 日志清理；
- 至少两台Windows电脑；
- README；
- 来源；
- 授权；
- 已知问题；
- 最终包。

## 附加

只允许低风险内容：

- 修正文案；
- 替换素材；
- 调整数值；
- 增加已完成模块的简单关卡；
- 不允许新增底层模块。

---

# 9. 9月5日交付

只处理阻塞问题，确认：

- `main` 与构建对应；
- S0为0；
- 无主流程S1；
- 三章节均有正式可玩内容；
- 1位重点科学家、3项成就；
- Windows包和源码齐全。

---

# 10. 9月5日后的完整开发

继续完成原设计：

- 长镜面；
- 两种分光器；
- RGB滤光片和颜色目标；
- 光形式转换器；
- 同时和顺序水晶；
- 墙体消除器；
- 光电二极管；
- 电控门；
- 电动镜面；
- 次级发射器；
- 特殊持续光线；
- 更多科学家；
- 更多成就；
- 补齐三章节关卡数量和难度曲线；
- 更完整UI、声音、动画、设置和存档。

9月5日前未完成的附加内容必须保留Issue、规则和原型，不视为永久删除。
